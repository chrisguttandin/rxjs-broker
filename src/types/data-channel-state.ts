// @todo This is an extract of @types/webrtc because it can't be used anymore since it causes a conflict with the built-in types.

export type TDataChannelState = 'closed' | 'closing' | 'connecting' | 'open';
